import styled from 'styled-components';

interface IProps {}

const Dormant: React.FC<IProps> = () => {
  return <S.container>Dormant</S.container>;
};

const S = {
  container: styled.div``,
};

export default Dormant;
